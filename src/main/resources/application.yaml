server:
  port: 8080
  servlet:
    context-path: /laptop

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/laptop"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

